# 注册

## 功能是什么

创建新用户账户。

## 对应代码在哪

- 代码位置：[useAuthStore.js](../../../src/store/useAuthStore.js)
  符号：`register`
  作用：前端注册动作。
- 代码位置：[LoginModal.jsx](../../../src/components/overlays/LoginModal.jsx)
  符号：`register mode`
  作用：注册模式表单与 UI 入口。
- 代码位置：[auth.controller.ts](../../../mde-server/src/auth/auth.controller.ts)
  符号：`register`
  作用：服务端注册接口。
- 代码位置：[auth.service.ts](../../../mde-server/src/auth/auth.service.ts)
  符号：`register`
  作用：创建用户并返回登录态。

## 关键代码怎么读

- 第一步看 [useAuthStore.js](../../../src/store/useAuthStore.js) 中的 `register`，其作用是：前端注册动作。
- 第二步看 [LoginModal.jsx](../../../src/components/overlays/LoginModal.jsx) 中的 `register mode`，其作用是：注册模式表单与 UI 入口。
- 第三步看 [auth.controller.ts](../../../mde-server/src/auth/auth.controller.ts) 中的 `register`，其作用是：服务端注册接口。
- 第四步看 [auth.service.ts](../../../mde-server/src/auth/auth.service.ts) 中的 `register`，其作用是：创建用户并返回登录态。

## 论文里是怎么定义这个功能的

- `2.3.2 用例表`
- `6.4.1`

## 协作建议

- 上级模块：[云同步模块](./F-03-云同步模块.md)
- 这是一份单功能文档，适合后续继续补“输入输出”“状态变化”“测试场景”“异常处理”。
- 如果后续要对应源码细讲，建议直接在本文件下新增“关键流程时序”小节。
